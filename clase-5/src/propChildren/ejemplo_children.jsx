import React from 'react'
import { FlexColumn } from './style'

// Ordene de forma Flex column
export const EjemploChildren = ({children}) => {
  return (
    <FlexColumn >
    {children}
    </FlexColumn>
  )
}
